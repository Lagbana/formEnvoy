// import { positions, Provider, useAlert } from "react-alert";
// import { isUndefined } from "lodash";

// export const webAlert = (props) => {
//   const { message } = props;

//   const alert = useAlert();

//   return alert.show(message);
// };
